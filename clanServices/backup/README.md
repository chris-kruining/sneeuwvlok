# Backup Clan service

Foundational backup service. The initial driver is `borg`; repositories, paths, exclusions, schedules, and SSH details are supplied by instance settings or explicit backup target inputs.
